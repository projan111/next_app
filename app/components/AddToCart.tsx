import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("serever component")}>
        Add To Cart
      </button>
    </div>
  );
};

export default AddToCart;
